{% extends 'base_back.html.twig' %}

{% block body %}




    <div class="mainContainer backContainer">
        <div class="form-container mx-auto">
            <div class="header-text my-3">
                <h1 class="text-center  ">Ajout du type de pet</h1>
                <p class="contactInfo my-3 text-center">Ajouter une espèce
                </p>
            </div>
            {{ form_start(PetTypeForm) }}
            <div class="row">
                <div class="col">
                    {{ form_row(PetTypeForm.name) }}
                </div>
                <div class="row w-75 mx-auto my-2 justify-content-center">
                    <button type="submit" class="btn btn-dark align-self-lg-center w-50">Envoyer</button>
                </div>
            </div>
            {{ form_end(PetTypeForm) }}
        </div>
    </div>

{% endblock %}